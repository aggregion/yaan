apiVersion: yaan.aggregion.com/v1
kind: Deployment
metadata:
  name: stage
  namespace: client

spec:
  title: "Client Stage Deployment"
  deploymentGroups:
    stage:
      type: KubernetesCluster
      cluster: k8s
      namespace: prod
      components:
        - backend
        - mongo
        - rabbitMQ
      relations:
        - namespace: client
          deployment: common
          deploymentGroup: database
          components:
            - clickHouse
