apiVersion: yaan.aggregion.com/v1
kind: Deployment
metadata:
  name: common

spec:
  title: "Client Common Deployment"
  deploymentGroups:
    db:
      type: Server
      server: db
      solution: myapp
      components:
        - clickHouse
